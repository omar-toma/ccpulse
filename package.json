{
  "name": "ccpulse-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r --filter './packages/core' build && pnpm -r --filter './packages/web' build && pnpm -r --filter './packages/daemon' build && pnpm -r --filter './packages/cli' build && tsx scripts/embed-web.ts",
    "dev:daemon": "pnpm --filter ccpulse-daemon dev",
    "dev:web": "pnpm --filter ccpulse-web dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
